# Scripts

Add reusable setup, training, evaluation, and analysis commands when their execution paths are established. Put nebula-specific launch commands under `compute/nebula/` once the environment and resource policy are known, and document their use in `../COMPUTE.md`.

No training launcher exists at workspace initialization.
