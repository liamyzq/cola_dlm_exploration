# Idea Methods

Place reusable loss, objective, schedule, or sampling changes here. Name modules after their actual behavior and link them from the owning idea README. Design composition when a concrete A+B experiment requires it; avoid introducing an abstract plugin framework before it is needed.
