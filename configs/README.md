# Configurations

Keep shared baseline and idea configurations here. Add `000_baseline/` and `<idea_id>/` directories as runnable protocols are implemented. Formal runs use a unique configuration associated with the implementation commit; store the complete resolved configuration with the run artifacts.

Choose the configuration format after inspecting the selected upstream implementation. No executable configuration or configuration framework is introduced during workspace setup.
