# <idea_id>: <Idea title>

Status: proposed.

## Question and hypothesis

State the mechanism being tested and why the expected behavior would follow. Identify the smallest observation that would support or weaken the hypothesis.

## Comparison

Name the baseline and its provenance. Specify the data, evaluation protocol, primary metric and direction, budget, and seeds that matter for the comparison. Describe any intended approximation as part of the actual protocol.

## Implementation

Link the shared method module and executable configuration when they exist. Describe the change relative to the shared baseline and whether it can combine with other ideas. Do not copy the full training or model implementation here.

## Runs and evidence

Before formal execution, record the experiment ID, implementation commit, config path, exact launch command, stopping condition, and artifact location. Link the corresponding entries in `../results.tsv` and `../../jobs/jobs.jsonl`. Keep full resolved configurations with the run outputs.

## Findings and decision

No experiments yet. After a run, distinguish observed results from interpretation and state the next action. Link negative or invalid outcomes as well as successful ones.
